<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('partials/meta') %>
</head>
  <body>
    <!-- nav -->
 <%- include('partials/navbar') %>
<!-- Carousel -->
 <%- include('partials/carousel') %>
    <!-- Intro -->
    <section class="container-fluid text-center">
      <h1 class="p-3 fs-2 border-top border-3">Estilos unicos para lograr el mejor Look</h1>
      <p>Nos encargamos de traerte los mejores diseños y lo mejor de la Moda.</p>
    </section>
    <!-- Catalogo -->
    <div class="d-grid" id="cat">
      <button class="btn btn-dark" type="button">Catalogo</button>
    </div> 
      <!-- Cards -->
  <main>   
    <section class="container">
      <div class="row justify-content-center">
        <% JSONBody.forEach(products =>{ %>
          <div class="card col-2 p-0 col-xs-6 col-sm-6 col-md-4 col-lg-4">
            <img src="<%= products.img %>" alt="<%= products.alt %>">
            <h4><%= products.name %></h4>
            <p><%= products.description %></p>
            <button type="button" class="btn btn-dark"><a href="#">Mas info</a></button>
          </div>
        <% }) %>
      </div>  
    </section>
  </main>
  <!-- Footerbreak -->
  <div class="d-grid">
    <button class="btn btn-dark mt-4 border-0" type="button" id="con">Contacto</button>
  </div>
  <!-- Footer -->
<%- include('partials/footer') %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous"></script>
  </body>
</html>
